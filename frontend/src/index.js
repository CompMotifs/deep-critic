import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './DeepCriticApp.css';  // Global CSS, or use a separate global CSS if preferred

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
